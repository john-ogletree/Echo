---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import Button from '../components/Button.astro';
import Card from '../components/Card.astro';
import Blog from '../components/Blog.astro';
import Projects from '../components/Projects.astro';
import Services from '../components/Services.astro';
import Profile from '../components/Profile.astro';
import ClientCard from '../components/ClientCard.astro';

// Data Imports
import projectsData from '../data/projects.json';
import blogData from '../data/blog.json';
import servicesData from '../data/services.json';
import skillsData from '../data/skills.json';
import bannerData from '../data/banner.json'; // Reusing for Hero content
import profileDataArray from '../data/profile.json';

// Data Processing
const latestProject = projectsData[projectsData.length - 1];

blogData.sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
const latestContent = blogData[0];

const mainProfile = profileDataArray.find(profile => profile.type === 'person');
const clients = profileDataArray.filter(profile => profile.type === 'client');

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout>
  <main>
    <Hero
      title={bannerData.title}
      subtitle={bannerData.subtitle}
      buttonText={bannerData.buttonText}
      buttonLink={bannerData.buttonLink}
    />

    <section class="section-container">
      {mainProfile &&
        <Profile
          name={mainProfile.name}
          bio={mainProfile.bio}
          avatar={mainProfile.avatar}
          buttonLink={mainProfile.buttonLink}
          skillsData={skillsData.slice(0, 4)} /* Display up to 4 skills */
          socialLinks={mainProfile.socialLinks}
        />
      }
    </section>

    <section class="section-container">
      <Services title="Our Expertise">
        {
          servicesData.map(category => (
            <Card
              type="service-category"
              title={category.title}
              description={category.description}
            />
          ))
        }
      </Services>
    </section>

    <section class="section-container">
      <Projects title="Featured Work">
        <Card
          type={latestProject.type}
          contentType={latestProject.contentType}
          title={latestProject.title}
          description={latestProject.description}
          image={latestProject.image}
          videoSrc={latestProject.videoSrc}
          author={latestProject.author}
          audioSrc={latestProject.audioSrc}
          link={latestProject.link}
        />
      </Projects>
    </section>

    <section class="section-container">
      <Blog title="Latest Insights">
        <Card
          type={latestContent.type}
          title={latestContent.title}
          description={latestContent.excerpt || latestContent.description}
          date={latestContent.date}
          link={latestContent.link}
          author={latestContent.author}
          rating={latestContent.rating}
          image={latestContent.image}
        />
      </Blog>
    </section>

    <section class="section-container">
      <h2 class="section-title">Our Valued Clients</h2>
      <div class="clients-grid">
        {
          clients.map(client => (
            <ClientCard
              name={client.name}
              bio={client.bio}
              avatar={client.avatar}
              link={client.link}
            />
          ))
        }
      </div>
    </section>

    <section class="section-container">
      <Card
        type="newsletter"
        title="Join Our Community"
        description="Subscribe to our newsletter for exclusive updates and content."
      />
    </section>
  </main>
</Layout>

<style>
  main {
    padding: 0; /* Remove main padding as sections will handle it */
  }

  .section-container {
    padding: 4rem 2rem; /* Consistent padding for all sections */
    border-bottom: 1px solid #1a1a1a; /* Subtle separator */
  }

  .section-container:last-of-type {
    border-bottom: none; /* No border for the last section */
  }

  .section-title {
    font-size: 2rem;
    text-align: center;
    margin-bottom: 3rem;
    color: #00e676; /* Vibrant green */
  }

  /* Client Grid Specific Styles */
  .clients-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    justify-items: center; /* Center items in the grid */
  }
</style>
